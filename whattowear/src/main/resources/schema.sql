create table wardrobe(
  id int auto_increment,
  title varchar(255) NOT NULL,
  waterproof boolean,
  marke varchar(255) NOT NULL
);

-- insert into wardrobe(title, waterproof, marke) values('test',true, 'Nike');